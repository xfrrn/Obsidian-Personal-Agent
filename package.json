{
  "name": "obsidian-personal-agent",
  "version": "0.1.0",
  "private": true,
  "description": "A safe, local-first personal knowledge agent for Obsidian.",
  "scripts": {
    "dev": "npm run build:agent-ui && node apps/obsidian-plugin/esbuild.config.mjs",
    "build": "npm run build:agent-ui && node apps/obsidian-plugin/esbuild.config.mjs production",
    "build:agent-ui": "npm --prefix apps/obsidian-plugin/ui run build",
    "package:windows": "powershell -NoProfile -ExecutionPolicy Bypass -File scripts/package-windows.ps1",
    "start:agent": "python -m agent.web.server --host 127.0.0.1 --port 8000",
    "check": "tsc --noEmit -p apps/obsidian-plugin/tsconfig.json",
    "test": "npm run test:plugin",
    "test:plugin": "esbuild apps/obsidian-plugin/tests/url.test.ts apps/obsidian-plugin/tests/file-suggestions.test.ts apps/obsidian-plugin/tests/code-actions.test.ts --bundle --platform=node --format=cjs --outdir=.test-build && node --test .test-build/url.test.js .test-build/file-suggestions.test.js .test-build/code-actions.test.js",
    "test:python": "python -m pytest -q -p no:cacheprovider -o asyncio_default_fixture_loop_scope=function tests/unit",
    "verify": "npm run check && npm run test:plugin && npm run test:python && npm run build"
  },
  "engines": {
    "node": ">=20"
  },
  "devDependencies": {
    "esbuild": "^0.28.1",
    "obsidian": "^1.13.1",
    "typescript": "^7.0.2"
  }
}
