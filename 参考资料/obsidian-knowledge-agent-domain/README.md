# Obsidian Knowledge Agent — Domain Package

本交付物包含 `packages/domain` 的完整第一版实现和中文设计文档。

- 代码：`packages/domain/src/oka_domain/`
- 测试：`packages/domain/tests/`
- 设计：`docs/domain-model-design.md`

验证命令：

```bash
cd packages/domain
PYTHONPATH=src python -m unittest discover -s tests -v
```
