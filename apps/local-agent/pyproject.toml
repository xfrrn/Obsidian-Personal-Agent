[project]
name = "obsidian-personal-agent-local-agent"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = ["httpx>=0.28,<0.29"]

[project.optional-dependencies]
package = ["pyinstaller>=6,<7"]

[tool.pytest.ini_options]
testpaths = ["tests"]
